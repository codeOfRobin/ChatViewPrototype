# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ChatViewPrototype' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ChatViewPrototype

  pod 'AsyncDisplayKit', :git => 'https://github.com/leotumwattana/AsyncDisplayKit', :branch => 'maxLinesToDisplay'


  target 'ChatViewPrototypeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ChatViewPrototypeUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
